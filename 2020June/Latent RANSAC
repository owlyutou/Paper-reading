研究问题：
基于观察结果：正确的假设在潜在参数域中紧密地聚集在一起。提出一种可以在恒定时间内评估RANSAC假设的方法(即与数据大小无关)，加速模型验证阶段。

研究方法：
在标准的RANSAC产生假设以后，为了明确定义变换空间的距离，将假设映射到latent space，将假设距离定义为匹配残差大小之差：
‖f_1-f_2 ‖_∞∝max┬(m∈M)⁡|err(f_1,m)-err(f_2,m)|
通过随机网格（Random Grids）哈希快速定位相似的假设对，以固定的时间检查先前生成的假设与当前假设是否“collides”（即他们是否足够接近），仅在发现一对假设后才执行假设验证阶段。
之后修改停止条件以确保以较高的概率得出第二个良好的假设。原来的终止条件是:n=(log⁡(1-p_0))/(log⁡(1-p))，修改了p_0（公式5和公式6）。
 
优点创新点：
优点：在保证准确率的同时提升了RANSAC的效率和速度（在恒定时间内处理假设）；在低内点率时候性能好；相比RHT可以处理更高维（8维）的问题。
创新点：距离定义为匹配残差大小之差；随机网格哈希。

思考：
随机网格哈希是是文献工作的主要创新（具体的哈希机制还没完全理解）；
文献还没有找到基础矩阵的适当参数化（可能是基础矩阵需要映射到更维的空间——H四点法、8维，常见的F估计七点法需要映射到更高维），这是未来的工作之一。
